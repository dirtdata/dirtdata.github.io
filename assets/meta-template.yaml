# meta.yaml TEMPLATE
slug: dataset-slug-thingy  # should already be implicit from meta.yaml's parent dir, but good to be explicit
title: Human_readable_headline
updated_at: YYYY-MM-DD

description: |
  Human_and_seo_friendly_description

files:
  - path: orignal.csv
    default: true

  - path: original.json

  - path: sample.csv
    description: |
      Why_does_this_sample_exist and what_is_in_it
    wrangle_script: |
      # just a way to record the code used to create the sample, just in case I need to repeate it
      cat original.csv | /path/to/script.that.wrangled  > sample.csv



license: name_of_license # e.g. CC0 1.0 Universal

publisher:
  name: Publisher_Name #.e.g. The @unitedstates project
  url: https://example.com/publisher/homepage

sources:
  # the sources object represents a list of canonical/common/expected URLs for any given
  # dataset, especially ones needed for the automation of re-fetching/mirroring data


  # homepage/landing page on which the data is linked from -- so user can be directed to
  # its original presentation context, and just incase we want to take a Wayback snapshot of it
  homepage: https://github.com/unitedstates/congress-legislators

  # A direct download URL to get the data – it should be curl-able without authentication
  download: https://theunitedstates.io/congress-legislators/legislators-current.csv


# `references` is where we list any other links germane to understanding the data
references:
  - title: Data dictionary
    url: https://github.com/unitedstates/congress-legislators#data-dictionary
